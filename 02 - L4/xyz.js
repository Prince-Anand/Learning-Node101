console.log("This is the xyz.js very important file.");

// All module code is wrapped in a function by Node.js

// IIFE - Immediately Invoked Function Expression
//  IIFE immediately invokes the function right after defining it.
// It keeps the variables and functions defined inside the IIFE private and keeps it safe
// (function (){})(); - This is the syntax of IIFE


(function (){
    // All module code is written here
})();